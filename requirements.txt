# AutoRUN
# Python 3.8+ 

# Core
pydantic>=2.0.0

# CLI UI
rich>=13.0.0
prompt-toolkit>=3.0.0
textual>=0.50.0

# Web UI
fastapi>=0.100.0
uvicorn>=0.23.0
websockets>=10.0

# Async
aiohttp>=3.8.0
httpx>=0.24.0

# SSH Remote (惰性导入，不影响 WebSocket)
# 以下版本兼容 Windows 7-11 + Python 3.8-3.13，无需 Rust 编译器
paramiko>=3.0.0,<4.0.0
cryptography>=41.0.0,<43.0.0
bcrypt>=4.0.0,<4.2.0

# Utilities
python-dotenv>=1.0.0

# OCR (optional — install falcon_perception from dev_sources/)
# pip install -e dev_sources/Falcon-Perception-main
# Dependencies: torch, transformers, torchvision, huggingface_hub, safetensors, tokenizers, Pillow
